# Image assets

`og-rackstat-placeholder.svg` is a temporary social-sharing card. Replace it with a tested 1200 × 630 PNG or JPEG when final product photography or a production enclosure render is available, then update the Open Graph and Twitter metadata in `index.html`.

Future development photographs should be exported at an appropriate display size, compressed, and loaded lazily when they appear below the fold.
